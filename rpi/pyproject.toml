[project]
name = "holub-detector"
version = "0.1.0"
description = "Detekce holubů z ESP32-CAM streamu pomocí YOLOv8"
requires-python = ">=3.11"
dependencies = [
    "ultralytics>=8.0",
    "requests>=2.28",
    "Pillow>=9.0",
]

[tool.uv]
torch-backend = "cpu"
